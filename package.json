{
  "name": "zitiernudel",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:api": "bun run --filter @zitiernudel/api dev",
    "dev:ext": "bun run --filter @zitiernudel/extension dev"
  }
}