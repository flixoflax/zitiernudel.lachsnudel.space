{
  "name": "zitiernudel",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:api": "bun run --filter api dev",
    "dev:ext": "bun run --filter zitier-nudel dev"
  }
}